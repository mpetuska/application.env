{
  "name": "application.env",
  "version": "1.0.4",
  "description": ".env file loader for node and browser",
  "repository": "github:mpetuska/application.env",
  "main": "common.js",
  "types": "build/index.d.ts",
  "author": "Martynas Petuška",
  "license": "Apache-2.0",
  "files": [
    "**/*.js",
    "**/*.d.ts",
    "**/*.map",
    "!.pnp.js",
    "!.yarn"
  ],
  "scripts": {
    "vscode": "yarn dlx @yarnpkg/pnpify --sdk vscode",
    "build": "tsc",
    "prepack": "yarn build"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.1",
    "@types/node": "^14.14.12",
    "typescript": "^4.1.2"
  }
}
